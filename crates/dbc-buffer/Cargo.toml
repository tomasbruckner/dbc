[package]
name = "dbc-buffer"
version = "0.1.0"
edition.workspace = true
description = "Columnar result buffer that streams rows without holding them all as strings"
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
dbc-core = { path = "../dbc-core" }
tempfile = "3"
tokio.workspace = true

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "push_1m"
harness = false
